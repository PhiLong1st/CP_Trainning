

/*
    Code by: KoKoDuDu
    Created: 26.10.2024 18:05:47
*/
#include <bits/stdc++.h>

using namespace std;

#define int long long
#define pll pair<int, int>

const int MOD = 1e9 + 7;

void solve() {
    int n;
    cin >> n;
    vector<string> a(n + 1);
    for (int i = 1; i <= n; ++i) {
        cin >> a[i];
        for (int j = 0; j < a[i].size(); ++j) {
            
        }
    }
}

int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t = 1;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}