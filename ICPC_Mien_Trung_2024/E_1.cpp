/*
    Code by: KoKoDuDu
    Created: 22.10.2024 17:20:59
*/
#include <bits/stdc++.h>

using namespace std;

#define int long long
#define pll pair<int, int>

const int MOD = 1e9 + 7;

void solve() {
    int n, m, s;
    cin >> n >> m >> s;
    vector<vector<char>> a(n, vector<char>(m));
    for (auto& v : a) for (auto& c : v) cin >> c;

}

int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t = 1;
    // cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}